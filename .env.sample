# OpenAI
OPENAI_API_KEY=

# Scientific papers databases (Optional)
# Additional databases to search for papers
BING_API_KEY=
CORE_API_KEY=
SERP_API_KEY=

# Langsmith API key (Optional)
# Add agent tracing and monitoring
LANGCHAIN_API_KEY=

# Redis URL (Optional)
# Cache for tool results
# Set to default for local deployment
REDIS_URL="redis://redis:6379"

# Database
# Postgres instance storing conversations and user data
# Set to default for local deployment
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/mydb?schema=public"

# Frontend and API URLs 
# Default for local deployment
FRONTEND_URL="http://localhost:3000"
NEXT_PUBLIC_WS_URL="ws://localhost:8000/ws"

# Auth
# Default for local deployment
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="my-secret"
